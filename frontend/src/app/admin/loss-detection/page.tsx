import { LossDetectionWorkspace } from '@/components/operations/LossDetectionWorkspace';

export default function AdminLossDetectionPage() {
  return (
    <LossDetectionWorkspace
      title="Loss Detection Desk"
      description="Mismatch checks, stale-parcel monitoring, and high-value claim approvals."
    />
  );
}
