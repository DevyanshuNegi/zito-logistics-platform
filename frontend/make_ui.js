const fs=require('fs'); const path=require('path'); const routes = ['src/app/(warehouse)/dashboard','src/app/(warehouse)/bins','src/app/(warehouse)/inventory','src/app/(warehouse)/scan','src/app/(admin)/loss-detection','src/app/(admin)/rto','src/app/(admin)/fleet']; routes.forEach(r => { fs.mkdirSync(r, { recursive: true }); fs.writeFileSync(path.join(r, 'page.tsx'), 'export default function Page() { return <div>' + r + '</div>; }'); });