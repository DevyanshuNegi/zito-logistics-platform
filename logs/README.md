y# Zito Logs

Keep temporary runtime, preview, and debug log files in this folder only.

Rules:
- do not create scattered `.log` files in `backend/`, `frontend/`, or other workspace folders
- use flattened filenames if needed, for example `backend__backend-dev.log`
- treat these logs as temporary local artifacts, not long-lived project files
